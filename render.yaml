services:
  - type: web
    name: phone-intel-backend
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: SEARCHBUG_ACCOUNT
        sync: false
      - key: SEARCHBUG_PASS
        sync: false
